# book-app-frontend

## Setup
1. Install dependencies:
	`npm install`
2. Copy `.env.example` to `.env.local`.
3. Set Firebase values and `VITE_API_BASE_URL`.

## Run
- Development: `npm run dev`
- Build: `npm run build`
- Preview build: `npm run preview`
